Stage all changes, create a commit with an appropriate message based on what changed, and push to the current branch.

Steps:
1. Run `git status` and `git diff` to understand what changed
2. Stage all modified and untracked files (excluding .env and other sensitive files)
3. Write a concise commit message that describes the changes
4. Commit and push to the current branch using `git push`
